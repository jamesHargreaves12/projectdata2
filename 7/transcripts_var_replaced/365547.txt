if VARIABLE_0 equals NUMBER then we return false if VARIABLE_0 equals NUMBER we return true else we start a for loop from VARIABLE_2 to the FUNCTION_CALL_5 of VARIABLE_0 and then inside the for loop we check that if the FUNCTION_CALL_6 of VARIABLE_0 an VARIABLE_1 equals NUMBER we return false and else we do nothing and then thats the end of the end of the for loop and then if nothing else happens then we return true
