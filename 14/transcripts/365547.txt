if the length of the list is smaller than equal to 1 we simply return the list now we compute some middle value which the length to list divided by 2 and then we before merge sort on left half which is the 0 th element of the list until the middle element minus 1 we merge the right half the list which is the middle element in the list until the length of the list minus 1 element